# Task Validation Bundle

- Change ID: `add-typora-wysiwyg-editor`
- Run: `#2` (`run-0002`)
- Task ID: `1.1`
- Ref ID: `R1`
- Scope: `GUI`

## Task

完成 WYSIWYG 编辑器内核选型并落地最小可编辑集成。要求单栏编辑区可直接编辑渲染结果，具备稳定光标和基础输入能力，同时保留双栏模式行为不变。

## How To Run

- macOS/Linux:
  - `bash auto_test_openspec/add-typora-wysiwyg-editor/run-0002__task-1.1__ref-R1__20260219T015420Z/run.sh`
- Windows:
  - `auto_test_openspec\add-typora-wysiwyg-editor\run-0002__task-1.1__ref-R1__20260219T015420Z\run.bat`

GUI hard rule applies: scripts are start-server-only and print access URL/port.

## Inputs / Outputs

- Inputs: none required by run scripts
- Outputs generated by run scripts: none (service startup only)
- Evidence artifacts to be captured by Supervisor during GUI validation:
  - screenshots under `outputs/screenshots/`
  - optional console/network logs under `logs/`

## GUI MCP Runbook

- Execute MCP-only runbook: `tests/gui_runbook_wysiwyg_minimal_editing.md`
- Do not use any executable browser automation scripts.

## Acceptance Criteria (Machine-Decidable Anchors)

- Server startup prints `http://127.0.0.1:33100/` and keeps serving until stopped.
- Via MCP runbook assertions in the same single-pane editor:
  - can place cursor and type text directly in rendered area
  - can select text and delete/backspace without cursor jump or editor crash
  - after editing in WYSIWYG mode, switching back to dual-pane reflects updated Markdown
  - dual-pane still shows `Markdown 输入` + `预览效果` with original two-column layout behavior
